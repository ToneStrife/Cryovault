/** Shared page layout class names for consistent responsive padding. */
export const PAGE_HEADER = 'px-4 sm:px-6 lg:px-8';
export const PAGE_BODY = 'px-4 sm:px-6 lg:px-8 py-4 lg:py-6';

/** Extra classes for DialogContent overrides (viewport-safe on mobile). */
export const DIALOG_MOBILE =
  'max-w-[calc(100vw-1.5rem)] sm:max-w-lg max-h-[min(90vh,calc(100dvh-2rem))] overflow-y-auto p-4 sm:p-6';
